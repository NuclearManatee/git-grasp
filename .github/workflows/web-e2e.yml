name: Web e2e

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  web-e2e:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4

      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.3.14

      - name: Install
        run: bun install

      - name: Export web pack
        run: bun scripts/export-web-pack.js
        env:
          GIT_HELP_MOCK_EMBEDDINGS: '0'

      - name: Build web
        run: bun run web:build

      - name: Install Playwright Chromium
        working-directory: apps/web
        run: bunx playwright install chromium --with-deps

      - name: Run e2e
        working-directory: apps/web
        run: bun run e2e
        env:
          CI: '1'
