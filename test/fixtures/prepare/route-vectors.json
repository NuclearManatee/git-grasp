{
  "description": "Frozen vectors for router fixture tests (no live OpenAI). Dim=4; taxonomy anchors are intentionally non-orthogonal so multi-anchor Δ can fire.",
  "dim": 4,
  "taxonomy": [
    { "command": "git status", "vector": [1, 0, 0, 0] },
    { "command": "git commit", "vector": [0.98, 0.2, 0, 0] },
    { "command": "git rebase", "vector": [0, 0, 1, 0] },
    { "command": "git log", "vector": [0, 0, 0, 1] }
  ],
  "chunks": [
    {
      "id": "near-status",
      "content": "Show the working tree status",
      "vector": [1, 0.01, 0, 0],
      "expect_commands": ["git status", "git commit"]
    },
    {
      "id": "near-rebase-only",
      "content": "Replay commits onto another tip",
      "vector": [0, 0, 1, 0],
      "expect_commands": ["git rebase"]
    },
    {
      "id": "noise",
      "content": "Unrelated cooking recipe",
      "vector": [0.1, 0.1, 0.1, 0.1],
      "expect_commands": []
    }
  ]
}
