// @ts-nocheck
/**
 * Legacy rank helpers removed â€” hybrid fusion lives in fusion.ts / hybrid.ts.
 * normalizeQuery re-exported for callers that still import from rank.
 */
export { normalizeQuery } from './hybrid.js';
