Write natural-language search queries only — never start with `git` or paste a full command line.
Do not wrap shell commands in backticks. Paraphrase errors/symptoms in human words; short error fragments are ok when the cell asks for them.
